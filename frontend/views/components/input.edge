<div class="form-group">
    <label for="{{ id }}" class="">{{ label ? label : '' }}</label>
    <input id="{{ id }}" name="{{ id }}" type="{{ type ? type : 'text' }}" placeholder="{{ placeholder? placeholder : '' }}" class="">
</div>
